product:
    type: jvm/app

dependencies:
    - ../transform
    - $libs.kotlinx.io.core
    - $libs.ktor.client

settings:
    publishing:
        name: icalendar

apply:
    - ../publish.module-template.yaml
