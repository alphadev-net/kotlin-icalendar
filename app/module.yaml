product:
    type: jvm/app

dependencies:
    - ../transform
    - $libs.ajalt.clikt
    - $libs.kotlinx.io.core
    - $ktor.client.cio

settings:
    publishing:
        name: icalendar
    ktor: enabled

apply:
    - ../publish.module-template.yaml
